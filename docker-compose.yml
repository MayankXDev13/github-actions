services:
  frontend:
    image: mayankxdev/github-actions-frontend-test
    restart: unless-stopped
    networks:
      - frontend

  nginx:
    image: nginx:latest
    restart: unless-stopped
    networks:
      - frontend
    ports:
      - "80:80"
    depends_on:
      - frontend
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

networks:
  frontend:
    driver: bridge